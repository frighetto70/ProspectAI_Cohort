# Story 3.1: Kanban Pipeline View

## Status: Ready for Review

## Story

> As a consultant,
> I want a Kanban board showing my prospects by stage,
> so that I can visualize my entire pipeline at a glance.

## Epic

Epic 3: Dashboard & Pipeline Management

## Acceptance Criteria

- [ ] 1. Página `/` apresenta visualização Kanban com colunas: Novo → Mensagem Gerada → Enviado → Respondeu → Converteu / Descartado
- [ ] 2. Cards de prospect mostram: nome, cargo, empresa, data da última ação
- [ ] 3. Drag-and-drop para mover prospects entre colunas (atualiza status via Server Action)
- [ ] 4. Contadores por coluna visíveis no header
- [ ] 5. Filtros globais aplicáveis ao Kanban (empresa, data, busca por texto)
- [ ] 6. Responsivo: colunas empilham em telas menores

## Dev Notes

### Components [Source: architecture.md#3]

```
components/kanban/
├── kanban-board.tsx        # Client Component ('use client') — @dnd-kit container
├── kanban-column.tsx       # Column with header counter + droppable area
└── prospect-card.tsx       # Draggable card with prospect summary
```

### Drag & Drop [Source: architecture.md#2]

- Biblioteca: `@dnd-kit/core` + `@dnd-kit/sortable`
- KanbanBoard é Client Component (requer interatividade)
- Dados iniciais vêm via Server Component (SSR) e são passados como props
- Optimistic update: mover card visualmente antes da resposta do servidor

### Server Action para Status Update [Source: architecture.md#5.2]

> **Nota:** Esta story cria `actions/prospect-actions.ts` com `updateProspectStatus`. Story 3.3 adiciona `addProspectNote` e `discardProspect` ao mesmo arquivo.

```typescript
// actions/prospect-actions.ts (criado nesta story)
'use server'
export async function updateProspectStatus(id: number, status: ProspectStatus) {
  // Update prospect status in Turso
  // Revalidate path '/'
}
```

### Status Columns

| Column | Status Value | Color (suggested) |
|--------|-------------|-------------------|
| Novo | `new` | Gray |
| Msg Gerada | `message_generated` | Blue |
| Enviado | `sent` | Yellow |
| Respondeu | `replied` | Green |
| Converteu | `converted` | Navy |
| Descartado | `discarded` | Red (muted) |

### Page Architecture

> **Evolução:** Story 1.4 criou `app/page.tsx` com tabela de prospects. Esta story evolui a página de tabela para visualização Kanban, mantendo filtros existentes.

```typescript
// app/page.tsx (Server Component) — evolução de tabela → Kanban
// 1. Fetch all prospects grouped by status
// 2. Fetch metrics (total, by status)
// 3. Render MetricsBar (Server) + KanbanBoard (Client, receives data as props)
```

### Responsiveness [Source: prd.md#3.6]

- Desktop: colunas lado a lado (horizontal scroll se necessário)
- Mobile: colunas empilhadas verticalmente, cards colapsados

## Tasks

- [x] 1. Instalar `@dnd-kit/core` e `@dnd-kit/sortable` (se ainda não instalados) (AC: 3)
- [x] 2. Criar `components/kanban/prospect-card.tsx` com nome, cargo, empresa, última ação (AC: 2)
- [x] 3. Criar `components/kanban/kanban-column.tsx` com header (nome + contador) + droppable area (AC: 1, 4)
- [x] 4. Criar `components/kanban/kanban-board.tsx` como Client Component com @dnd-kit (AC: 1, 3)
- [x] 5. Implementar drag-and-drop handler que chama Server Action `updateProspectStatus` (AC: 3)
- [x] 6. Implementar optimistic update no drag (AC: 3)
- [x] 7. Atualizar `app/page.tsx` para buscar prospects agrupados e renderizar Kanban (AC: 1)
- [x] 8. Integrar SearchFilters existente com o Kanban (AC: 5)
- [x] 9. Implementar CSS responsivo: horizontal em desktop, vertical em mobile (AC: 6)
- [x] 10. Clicar no card navega para `/prospects/[id]` (AC: 2)

## File List

- `components/kanban/kanban-board.tsx` — Client Component com DndContext, optimistic updates
- `components/kanban/kanban-column.tsx` — Coluna com header (título + contador) + droppable area
- `components/kanban/prospect-card.tsx` — Card draggable com dados do prospect + link
- `actions/prospect-actions.ts` — Server Action updateProspectStatus com revalidatePath
- `app/(dashboard)/page.tsx` — Atualizado de tabela para Kanban board
